# ===== DOMAIN =====
DOMAIN=localhost,127.0.0.1,mydomain.com
ALLOWED_HOSTS=localhost,127.0.0.1,mydomain.com
CSRF_TRUSTED_ORIGINS=localhost,127.0.0.1,mydomain.com

# ========== ОБЩЕЕ ==========
SECRET_KEY=django-insecure-v8idlx6j_bo*k1c)*p+5d1$k2qc0r3f123+im@_a!qkkp&srd@

# === PROJECT NAME ===
PROJECT_NAME=name

# === POSTGRES ===
POSTGRES_DB=name
POSTGRES_USER=name_user
POSTGRES_PASSWORD=name1234
POSTGRES_HOST=db_name
POSTGRES_PORT=5432
